<nav class="navbar navbar-light bg-transparent border-bottom box-shadow mb-3">
    <div class="container-fluid">
        <!-- Left-aligned items (e.g. navigation links) -->
        <ul class="navbar-nav d-flex flex-row">
            <!-- Link to view all fitness classes -->
            <li class="nav-item">
                <a class="nav-link text-white btn btn-outline-success rounded-pill px-4 py-2" asp-area="" asp-controller="Product" asp-action="All">Продукти</a>
            </li>

            <li class="nav-item">
                <a class="nav-link text-white btn btn-outline-success rounded-pill px-4 py-2" asp-area="" asp-controller="CommercialSite" asp-action="All">Търговски обекти</a>
            </li>

            @if (this.User?.Identity?.IsAuthenticated ?? false)
            {
                @if (User.IsAdmin())
                {
                    <li class="nav-item">
                        <a class="nav-link text-white btn btn-outline-success rounded-pill px-4 py-2" asp-area="Admin" asp-controller="Product" asp-action="Add">Добави продукт</a>
                    </li>
                }
            }

        </ul>
    </div>
</nav>
